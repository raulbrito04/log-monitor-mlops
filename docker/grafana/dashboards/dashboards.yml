apiVersion: 1

providers:
  - name: "LogMonitor Dashboards"
    orgId: 1
    folder: "Log Monitor"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
